No need to delete and start again — you're actually doing great! I can see the src folder is already there. Let's just add the missing files.
Here's what you still need to add:

src/index.js — inside the src folder
public/index.html — in a new public folder
package.json — in the root


To add src/index.js:

Click the src folder
Click Add file → Create new file
In the name box, type index.js
Open your index.js file with Notepad/TextEdit, copy all the text, paste it in
Click Commit new file


To add public/index.html:

Go back to the main repository page
Click Add file → Create new file
In the name box type public/index.html — GitHub will automatically create the public folder
Open your index.html file, copy all the text, paste it in
Click Commit new file


To add package.json:

Go back to the main repository page
Click Add file → Create new file
Type package.json in the name box
Open your package.json file, copy all the text, paste it in
Click Commit new file


Start with src/index.js and let me know when you're done with each one!
